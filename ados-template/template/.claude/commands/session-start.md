# /project:session-start

1. Read `docs/NOW.md`.
2. Read the active pack `INDEX.md` and `cursor.md`.
3. Read `docs/foundation/overview.md` if the active seam depends on shared canon.
4. Read the active pack `requirements.md` and `plan.md` as needed.
5. Read nearest path-scoped `CLAUDE.md` when working in monorepos.
6. Check environment (`git status`, branch, required services).
7. Confirm workspace scope for this task (package/app/path) in monorepos.
8. Evaluate task (requirements, scope, dependencies, test strategy).
9. Propose expected close mode (`lite|standard|full`) based on risk.
10. Present concise plan and wait for confirmation before coding on non-trivial work.
